#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    ifstream fin("text.txt");

    int cnt = 0;
    string line;

    while (getline(fin, line)) {
        cnt++;
    }

    cout << cnt;
    return 0;
}
